# 📋 Рабочий процесс выполнения заданий

## Исправленная проблема

**Было:** При взятии задания оно сразу отправлялось на модерацию без возможности прикрепить скриншоты и написать отчёт.

**Стало:** Правильный workflow с промежуточным статусом "Взято".

---

## 🔄 Новый процесс выполнения задания

### 1. Взятие задания
- Пользователь выбирает задание на главной странице
- Выбирает рабочий аккаунт
- Нажимает "Взять задание"
- **Статус:** `taken` (Взято) - синий бейдж 🔵

### 2. Выполнение задания
- Пользователь выполняет задание
- Переходит в "Мои задания"
- Видит задание со статусом "Взято"
- Форма для отправки отчёта доступна

### 3. Отправка отчёта
- Пользователь пишет описание выполнения
- Прикрепляет скриншоты (до 5 штук)
- Нажимает "Отправить на проверку"
- **Статус:** `pending` (На проверке) - жёлтый бейдж 🟡

### 4. Модерация
- Админ видит задание в разделе "Модерация выполненных заданий"
- Проверяет отчёт и скриншоты
- Одобряет или отклоняет

### 5. Результат
- **Одобрено:** `approved` (Одобрено) - зелёный бейдж 🟢
  - Награда начислена на баланс
  - Если есть реферер - ему начислен бонус
- **Отклонено:** `rejected` (Отклонено) - красный бейдж 🔴
  - Можно отправить отчёт заново
  - Слот возвращается в задание

---

## 🎨 Статусы заданий

| Статус | Цвет | Описание | Действия |
|--------|------|----------|----------|
| `taken` | 🔵 Синий | Задание взято | Можно отправить отчёт |
| `pending` | 🟡 Жёлтый | На проверке | Ожидание модерации |
| `approved` | 🟢 Зелёный | Одобрено | Награда начислена |
| `rejected` | 🔴 Красный | Отклонено | Можно переотправить |

---

## 💻 Технические изменения

### Server (server/server.js)
```javascript
// При взятии задания
INSERT INTO submissions (task_id, user_id, work_account_id, status) 
VALUES (?, ?, ?, 'taken')

// При отправке отчёта
UPDATE submissions SET proof_text = ?, proof_image = ?, status = 'pending' 
WHERE id = ? AND user_id = ?
```

### Dashboard (views/dashboard.ejs)
```javascript
// Форма показывается для статусов:
task.status === 'taken' || task.status === null || task.status === 'rejected'

// Статусы:
'taken': 'Взято'
'pending': 'На проверке'
'approved': 'Одобрено'
'rejected': 'Отклонено'
```

### CSS (public/css/dashboard.css)
```css
.task-status.taken {
  background: rgba(47, 128, 255, 0.1);
  color: var(--primary-blue);
}
```

---

## ✅ Проверка работы

1. Войдите как пользователь (test@test.com / test123)
2. Перейдите на главную страницу
3. Выберите задание и нажмите "Взять задание"
4. Выберите рабочий аккаунт
5. Перейдите в "Мои задания"
6. Увидите задание со статусом "Взято" (синий)
7. Заполните форму и прикрепите скриншоты
8. Нажмите "Отправить на проверку"
9. Статус изменится на "На проверке" (жёлтый)
10. Войдите как админ и промодерируйте

---

## 🐛 Исправленные баги

1. ✅ Задание сразу отправлялось на модерацию
2. ✅ Невозможно было прикрепить скриншоты
3. ✅ Невозможно было написать отчёт
4. ✅ Дублирование CSS класса .task-status.pending

---

**Дата исправления:** 2024-02-22  
**Коммит:** c454672  
**Статус:** ✅ Исправлено и протестировано
