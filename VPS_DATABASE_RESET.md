# Сброс базы данных на VPS

## 📋 Инструкция по сбросу базы данных YanFarm на сервере

### Шаг 1: Подключитесь к VPS

```bash
ssh root@ваш_ip_адрес
```

### Шаг 2: Перейдите в директорию проекта

```bash
cd /var/www/yanfarm
# или
cd /root/yanfarm
# (зависит от того, где установлен проект)
```

### Шаг 3: Остановите сервер

```bash
pm2 stop yanfarm
```

### Шаг 4: Обновите код с GitHub

```bash
git pull origin main
```

### Шаг 5: Запустите скрипт сброса базы данных

```bash
node reset-database.js
```

Вы увидите вывод:
```
🔄 Сброс базы данных YanFarm...

✓ Старая база данных удалена
✓ Новая база данных создана

Создание таблиц...

✓ Таблица users создана
✓ Таблица tasks создана
✓ Таблица submissions создана
✓ Таблица withdrawals создана
✓ Таблица work_accounts создана

Создание администратора...
✓ Администратор создан
  Email: admin@bigrussia.ru
  Пароль: admin123
  Реферальный код: XXXXXXXX

Создание тестовых заданий...
✓ Создано 5 тестовых заданий

✅ Сброс базы данных завершён!

📝 Теперь перезапустите сервер:

   Локально: npm start
   На VPS: pm2 restart yanfarm
```

### Шаг 6: Перезапустите сервер

```bash
pm2 restart yanfarm
```

### Шаг 7: Проверьте статус

```bash
pm2 status
pm2 logs yanfarm --lines 20
```

---

## 🔧 Альтернативный способ (одной командой)

Если вы хотите выполнить всё одной командой:

```bash
cd /var/www/yanfarm && pm2 stop yanfarm && git pull origin main && node reset-database.js && pm2 restart yanfarm && pm2 logs yanfarm --lines 20
```

---

## ⚠️ ВАЖНО!

**Этот скрипт удаляет ВСЕ данные:**
- Всех пользователей (кроме админа)
- Все задания (создаются новые тестовые)
- Все выполнения заданий
- Все заявки на вывод
- Все рабочие аккаунты

**Используйте только если:**
- Вы хотите начать с чистой базы
- Вы делаете тестирование
- Произошла критическая ошибка в базе данных

---

## 📊 Что создаётся после сброса

### Администратор
- Email: `admin@bigrussia.ru` (или из .env)
- Пароль: `admin123` (или из .env)
- Реферальный код: генерируется автоматически

### Тестовые задания (5 шт.)
1. Оставить отзыв на Яндекс.Картах - 25₽ (реферальная: 2.5₽)
2. Оставить отзыв на Google Maps - 30₽ (реферальная: 3₽)
3. Оценить товар на Wildberries - 120₽ (реферальная: 12₽)
4. Оставить отзыв на Ozon - 45₽ (реферальная: 4.5₽)
5. Отзыв на 2ГИС - 20₽ (реферальная: 2₽)

---

## 🔍 Проверка после сброса

Откройте в браузере:
- Главная: `https://yanfarm.pro`
- Админ-панель: `https://yanfarm.pro/admin`
- Партнёры: `https://yanfarm.pro/partners`

Войдите в админ-панель с учётными данными выше.

---

## 🆘 Если что-то пошло не так

### Сервер не запускается
```bash
pm2 logs yanfarm --lines 50
```

### База данных не создалась
```bash
ls -la database.db
node reset-database.js
```

### Нужно вернуть старую базу
Если вы сделали бэкап:
```bash
cp database.db.backup database.db
pm2 restart yanfarm
```

---

## 💾 Рекомендация: Создайте бэкап перед сбросом

```bash
# Создать бэкап
cp database.db database.db.backup.$(date +%Y%m%d_%H%M%S)

# Посмотреть список бэкапов
ls -lh database.db.backup.*

# Восстановить из бэкапа
cp database.db.backup.20240222_150000 database.db
pm2 restart yanfarm
```

---

## 📝 Настройка админа через .env

Если хотите изменить данные админа, отредактируйте `.env`:

```bash
nano .env
```

Добавьте или измените:
```
ADMIN_EMAIL=your@email.com
ADMIN_PASSWORD=your_secure_password
```

Затем запустите сброс:
```bash
node reset-database.js
```
